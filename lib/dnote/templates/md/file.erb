<%= title %>

<%= '=' * title.size %>

<% notes.by_file.each do |file, list| %>

file://<%= file %>
<%= '-' * (file.size + 7) %>

<% list.each do |note| %>
* <%= note %> (<%= note.line %>)
<% if note.code? %><%= "\n" + note.code.tabset(4).rstrip + "\n\n" %><% end %>
<% end; end %>

